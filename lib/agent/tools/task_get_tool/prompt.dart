const description = 'Get full details of a task by ID.';

const prompt = '''Retrieves full details of a task by its ID.

Returns: id, subject, description, status, blocks, blockedBy, and metadata.
Use TaskList to see all tasks in summary form first.''';
