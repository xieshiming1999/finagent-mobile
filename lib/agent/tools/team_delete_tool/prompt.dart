const description = 'Delete a team and stop all its members.';

const prompt = '''Delete a team and clean up all associated resources.

This will stop all running team members and remove the team context.
All team members must have completed or will be force-stopped.

Usage:
  TeamDelete(team_name: "icbc_analysis")
''';
