const description = 'List all tasks with summary information.';

const prompt = '''Lists all tasks with their summary information.

Returns each task's id, subject, status, owner, and blockedBy.
Use TaskGet with a specific task ID to view full details.''';
